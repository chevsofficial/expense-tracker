# Expense Tracker

Web-first expense tracking app (Next.js App Router + Tailwind + DaisyUI).

## Requirements
- Node.js (LTS recommended)
- npm

## Local dev
1) Install deps
```bash
npm install

Local sanity check: node scripts/test-mongo.mjs